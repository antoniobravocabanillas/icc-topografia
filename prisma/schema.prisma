generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["icc"]
}

enum Role {
  CUSTOMER
  TECHNICIAN
  SALES
  EDITOR
  ADMIN
  SUPER_ADMIN
  COMMERCIAL_ADMIN
  SURVEYOR
  ENGINEER
  ARCHITECT
  SUPPORT

  @@schema("icc")
}

enum OrderStatus {
  PENDING
  QUOTED
  PAID
  PROCESSING
  SHIPPED
  COMPLETED
  CANCELLED

  @@schema("icc")
}

enum LeadStatus {
  NEW
  CONTACTED
  QUALIFIED
  EVALUATION
  QUOTED
  NEGOTIATION
  WON
  LOST
  REQUIRES_TECH_SUPPORT

  @@schema("icc")
}

enum LeadPriority {
  LOW
  MEDIUM
  HIGH
  URGENT

  @@schema("icc")
}

enum OpportunityStatus {
  OPEN
  DISCOVERY
  PROPOSAL
  NEGOTIATION
  WON
  LOST
  ARCHIVED

  @@schema("icc")
}

enum SaleStatus {
  PENDING
  CONFIRMED
  INVOICED
  PARTIALLY_PAID
  PAID
  CANCELLED

  @@schema("icc")
}

enum CommissionType {
  SALE_PERCENTAGE
  MARGIN_PERCENTAGE
  FIXED_AMOUNT
  CATEGORY_PERCENTAGE

  @@schema("icc")
}

enum QuoteStatus {
  DRAFT
  SENT
  VIEWED
  ACCEPTED
  REJECTED
  EXPIRED
  CONVERTED

  @@schema("icc")
}

enum CommissionStatus {
  PENDING
  APPROVED
  PAID
  CANCELLED

  @@schema("icc")
}

enum ProjectStatus {
  PLANNING
  IN_PROGRESS
  FINISHED
  PUBLISHED
  ARCHIVED

  @@schema("icc")
}

enum TaskStatus {
  TODO
  IN_PROGRESS
  BLOCKED
  DONE
  CANCELLED

  @@schema("icc")
}

enum MilestoneStatus {
  PENDING
  IN_PROGRESS
  COMPLETED
  DELAYED

  @@schema("icc")
}

enum ProjectRole {
  SURVEYOR
  ENGINEER
  ARCHITECT
  SELLER
  SUPPORT
  MANAGER

  @@schema("icc")
}

enum TechnicalAvailability {
  AVAILABLE
  FIELD
  BUSY
  OFFLINE

  @@schema("icc")
}

enum TicketCategory {
  EQUIPMENT
  SERVICE
  CALIBRATION
  REPAIR
  WARRANTY
  TECHNICAL_QUERY
  OTHER

  @@schema("icc")
}

enum TicketPriority {
  LOW
  MEDIUM
  HIGH
  URGENT

  @@schema("icc")
}

enum TicketStatus {
  OPEN
  REVIEWING
  WAITING_CUSTOMER
  IN_PROGRESS
  RESOLVED
  CLOSED

  @@schema("icc")
}

enum BotProvider {
  LOCAL
  OPENROUTER
  GEMINI
  GROQ
  HUGGINGFACE
  OLLAMA

  @@schema("icc")
}

enum BotQuestionStatus {
  PENDING
  APPROVED
  REJECTED

  @@schema("icc")
}

enum NotificationType {
  LEAD
  QUOTE
  TICKET
  CHAT
  PROJECT
  COMMISSION
  FAQ
  SYSTEM

  @@schema("icc")
}

enum ActivityAction {
  CREATED
  UPDATED
  DELETED
  STATUS_CHANGED
  CONVERTED
  ASSIGNED
  COMMENTED
  UPLOADED
  APPROVED
  REJECTED

  @@schema("icc")
}

enum ChatStatus {
  WAITING
  ACTIVE
  CLOSED

  @@schema("icc")
}

enum StaffDepartment {
  SALES
  TECHNICAL_SUPPORT
  FIELD_ENGINEERING
  RENTAL_SERVICE
  ADMINISTRATION

  @@schema("icc")
}

enum TerraqoWorkspaceType {
  CLIENT_COMPANY
  INTERNAL_UNIT
  PRODUCT_ADMIN

  @@schema("icc")
}

enum TerraqoPlanTier {
  FREE
  BASIC
  PROFESSIONAL
  PREMIUM
  ENTERPRISE

  @@schema("icc")
}

enum TerraqoSubscriptionStatus {
  TRIALING
  ACTIVE
  PAST_DUE
  SUSPENDED
  CANCELLED

  @@schema("icc")
}

enum TerraqoModuleCode {
  CRM
  PROJECTS
  PUBLIC_WEBSITE
  TECHNICAL_STORE
  CUSTOMER_CHAT
  PROFESSIONAL_NETWORK
  LIVE_CV
  JOB_MARKETPLACE
  FORUMS
  PROFESSIONAL_MESSAGING
  TERRAQO_MEET
  COLLABORATION_TEAMS
  ANALYTICS
  AUTOMATIONS
  DOCUMENTS

  @@schema("icc")
}

enum TerraqoMeetingStatus {
  LIVE
  ENDED
  CANCELLED

  @@schema("icc")
}

enum TerraqoMeetingProvider {
  JITSI
  CUSTOM_JITSI

  @@schema("icc")
}

enum TerraqoConversationType {
  DIRECT
  COMPANY
  PROJECT
  GROUP

  @@schema("icc")
}

enum TerraqoConversationParticipantRole {
  OWNER
  MODERATOR
  MEMBER

  @@schema("icc")
}

enum TerraqoTeamStatus {
  ACTIVE
  ARCHIVED

  @@schema("icc")
}

enum TerraqoTeamRole {
  OWNER
  LEAD
  MEMBER

  @@schema("icc")
}

enum TerraqoTeamMemberStatus {
  INVITED
  ACTIVE
  DECLINED
  REMOVED

  @@schema("icc")
}

enum TerraqoMemberRole {
  OWNER
  ADMIN
  MANAGER
  MEMBER
  VIEWER
  CLIENT
  PROFESSIONAL

  @@schema("icc")
}

enum TerraqoProfessionalStatus {
  AVAILABLE
  WORKING
  OPEN_TO_PROJECTS
  NOT_AVAILABLE

  @@schema("icc")
}

enum TerraqoVisibility {
  PUBLIC
  COMMUNITY
  WORKSPACE
  PRIVATE

  @@schema("icc")
}

enum TerraqoMessagePrivacy {
  EVERYONE
  WORKSPACE
  FRIENDS
  NOBODY

  @@schema("icc")
}

enum TerraqoWorklogType {
  FIELD_UPDATE
  DELIVERABLE
  PROBLEM_SOLVED
  LEARNING
  MILESTONE

  @@schema("icc")
}

enum TerraqoEvidenceStatus {
  DECLARED
  LINKED
  CONFIRMED
  VERIFIED

  @@schema("icc")
}

enum TerraqoWorklogValidationStatus {
  REQUESTED
  APPROVED
  REJECTED
  CANCELLED

  @@schema("icc")
}

enum TerraqoWebAuthnPurpose {
  REGISTRATION
  WORKLOG_VALIDATION
  ATTENDANCE

  @@schema("icc")
}

enum TerraqoAttendanceType {
  CHECK_IN
  CHECK_OUT

  @@schema("icc")
}

enum TerraqoAttendanceStatus {
  ACCEPTED
  OUTSIDE_GEOFENCE
  LOCATION_UNAVAILABLE
  REJECTED

  @@schema("icc")
}

enum TerraqoReactionType {
  USEFUL
  INSIGHTFUL
  RESPECT

  @@schema("icc")
}

enum TerraqoApplicationStatus {
  DRAFT
  SUBMITTED
  REVIEWING
  SHORTLISTED
  ACCEPTED
  REJECTED
  WITHDRAWN

  @@schema("icc")
}

enum TerraqoIdentityVerificationStatus {
  PENDING_DOCUMENTS
  UNDER_REVIEW
  VERIFIED
  REJECTED

  @@schema("icc")
}

enum TerraqoProfessionalDocumentType {
  CV
  DNI_FRONT
  DNI_BACK
  CERTIFICATE
  PROFESSIONAL_LICENSE
  CRIMINAL_RECORD
  MEDICAL_EXAM
  BANK_CERTIFICATE
  OTHER

  @@schema("icc")
}

enum TerraqoDocumentReviewStatus {
  SUBMITTED
  VERIFIED
  REJECTED

  @@schema("icc")
}

enum TerraqoProfileEntryVerificationStatus {
  NOT_REQUESTED
  REQUESTED
  APPROVED
  REJECTED

  @@schema("icc")
}

enum TerraqoNoteKind {
  SIMPLE
  SECURE

  @@schema("icc")
}

enum TerraqoFileVisibility {
  PRIVATE
  WORKSPACE

  @@schema("icc")
}

enum TerraqoFileCategory {
  PLAN
  REPORT
  SPREADSHEET
  PRESENTATION
  IMAGE
  SOURCE_FILE
  CONTRACT
  TEMPLATE
  OTHER

  @@schema("icc")
}

model User {
  id                          String                           @id @default(cuid())
  name                        String?
  email                       String                           @unique
  emailVerified               DateTime?
  image                       String?
  passwordHash                String?
  role                        Role                             @default(CUSTOMER)
  accounts                    Account[]
  sessions                    Session[]
  orders                      Order[]
  addresses                   Address[]
  favorites                   Favorite[]
  assignedChats               ChatConversation[]
  ticketMessages              TicketMessage[]
  internalMessages            InternalChatMessage[]
  notifications               Notification[]
  activityLogs                ActivityLog[]
  staffProfiles               StaffProfile[]
  clientProfiles              Client[]
  clientAccounts              ClientAccount[]
  terraqoMemberships          TerraqoWorkspaceMember[]
  ownedTerraqoWorkspaces      TerraqoWorkspace[]               @relation("TerraqoWorkspaceOwner")
  terraqoProfessionalProfile  TerraqoProfessionalProfile?
  terraqoForumPosts           TerraqoForumPost[]
  terraqoForumReplies         TerraqoForumReply[]
  terraqoApplications         TerraqoProjectApplication[]
  terraqoWorklogs             TerraqoWorklogEntry[]            @relation("TerraqoWorklogAuthor")
  terraqoWorklogComments      TerraqoWorklogComment[]
  terraqoWorklogReactions     TerraqoWorklogReaction[]
  terraqoConversationsCreated TerraqoConversation[]            @relation("TerraqoConversationCreator")
  terraqoConversationMembers  TerraqoConversationParticipant[]
  terraqoDirectMessages       TerraqoDirectMessage[]           @relation("TerraqoDirectMessageSender")
  terraqoMeetingsCreated      TerraqoMeeting[]                 @relation("TerraqoMeetingCreator")
  terraqoMeetingParticipants  TerraqoMeetingParticipant[]
  terraqoTeamsOwned           TerraqoTeam[]                    @relation("TerraqoTeamOwner")
  terraqoTeamMemberships      TerraqoTeamMember[]              @relation("TerraqoTeamMemberUser")
  terraqoTeamInvitationsSent  TerraqoTeamMember[]              @relation("TerraqoTeamMemberInviter")
  terraqoPrivateNotes         TerraqoPrivateNote[]
  terraqoWorkspaceFiles       TerraqoWorkspaceFile[]
  terraqoPasskeys             TerraqoWebAuthnCredential[]
  terraqoWebAuthnChallenges   TerraqoWebAuthnChallenge[]
  terraqoValidationRequests   TerraqoWorklogValidation[]       @relation("TerraqoValidationRequester")
  terraqoValidationsAssigned  TerraqoWorklogValidation[]       @relation("TerraqoValidationSupervisor")
  terraqoAttendanceEvents     TerraqoAttendanceEvent[]
  createdAt                   DateTime                         @default(now())
  updatedAt                   DateTime                         @updatedAt

  @@schema("icc")
}

model Company {
  id                       String                           @id @default(cuid())
  legalName                String
  tradeName                String?
  publicSlug               String?
  document                 String?
  email                    String?
  phone                    String?
  address                  String?
  city                     String?
  region                   String?
  country                  String                           @default("PE")
  locationSubdivisionCode  String?
  locationCity             String?
  industry                 String?
  website                  String?
  logoUrl                  String?
  status                   String                           @default("prospecto")
  contacts                 Contact[]
  accounts                 ClientAccount[]
  clients                  Client[]
  leads                    Lead[]
  opportunities            Opportunity[]
  quotes                   Quote[]
  sales                    Sale[]
  projects                 Project[]
  tickets                  Ticket[]
  documents                Document[]
  activities               ActivityLog[]
  terraqoWorkspaces        TerraqoWorkspace[]
  professionalAffiliations TerraqoProfessionalAffiliation[]
  terraqoWorkspaceId       String
  terraqoWorkspace         TerraqoWorkspace                 @relation("TerraqoWorkspaceCompanies", fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt                DateTime                         @default(now())
  updatedAt                DateTime                         @updatedAt
  deletedAt                DateTime?

  @@index([terraqoWorkspaceId, publicSlug])
  @@index([terraqoWorkspaceId, document])
  @@index([document])
  @@index([country, locationSubdivisionCode, locationCity])
  @@index([status])
  @@index([terraqoWorkspaceId])
  @@index([deletedAt])
  @@schema("icc")
}

model Contact {
  id                 String           @id @default(cuid())
  companyId          String
  company            Company          @relation(fields: [companyId], references: [id], onDelete: Cascade)
  name               String
  roleTitle          String?
  email              String?
  phone              String?
  whatsapp           String?
  isPrimary          Boolean          @default(false)
  clientAccounts     ClientAccount[]
  leads              Lead[]
  opportunities      Opportunity[]
  quotes             Quote[]
  sales              Sale[]
  tickets            Ticket[]
  documents          Document[]
  activities         ActivityLog[]
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt
  deletedAt          DateTime?

  @@unique([companyId, email])
  @@index([email])
  @@index([terraqoWorkspaceId])
  @@index([deletedAt])
  @@schema("icc")
}

model ClientAccount {
  id                 String           @id @default(cuid())
  userId             String?
  user               User?            @relation(fields: [userId], references: [id], onDelete: SetNull)
  companyId          String
  company            Company          @relation(fields: [companyId], references: [id], onDelete: Cascade)
  contactId          String?
  contact            Contact?         @relation(fields: [contactId], references: [id], onDelete: SetNull)
  clientId           String?          @unique
  client             Client?          @relation(fields: [clientId], references: [id], onDelete: SetNull)
  status             String           @default("invited")
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  invitedAt          DateTime?
  lastLoginAt        DateTime?
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt
  deletedAt          DateTime?

  @@unique([terraqoWorkspaceId, userId])
  @@index([companyId])
  @@index([contactId])
  @@index([status])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model Client {
  id                 String           @id @default(cuid())
  userId             String?
  user               User?            @relation(fields: [userId], references: [id], onDelete: SetNull)
  companyId          String?
  companyRef         Company?         @relation(fields: [companyId], references: [id], onDelete: SetNull)
  account            ClientAccount?
  name               String
  company            String?
  document           String?
  email              String
  phone              String?
  address            String?
  country            String           @default("PE")
  region             String?
  locationSubdivisionCode String?
  locationCity       String?
  contactName        String?
  status             String           @default("prospecto")
  leads              Lead[]
  quotes             Quote[]
  projects           Project[]
  tickets            Ticket[]
  documents          ClientDocument[]
  sales              Sale[]
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt
  deletedAt          DateTime?

  @@unique([terraqoWorkspaceId, userId])
  @@unique([terraqoWorkspaceId, email])
  @@index([terraqoWorkspaceId, document])
  @@index([email])
  @@index([country, locationSubdivisionCode, locationCity])
  @@index([status])
  @@index([companyId])
  @@index([terraqoWorkspaceId])
  @@index([deletedAt])
  @@schema("icc")
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?
  user              User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@schema("icc")
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@schema("icc")
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
  @@schema("icc")
}

model Category {
  id                 String           @id @default(cuid())
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  name               String
  slug               String
  description        String?
  parentId           String?
  parent             Category?        @relation("CategoryTree", fields: [parentId], references: [id])
  children           Category[]       @relation("CategoryTree")
  products           Product[]
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt

  @@unique([terraqoWorkspaceId, slug])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model Product {
  id                 String           @id @default(cuid())
  name               String
  slug               String
  sku                String
  brand              String
  model              String?
  summary            String
  description        String
  price              Decimal?
  stock              Int              @default(0)
  currency           String           @default("USD")
  requiresQuote      Boolean          @default(false)
  availability       String
  badge              String?
  images             String[]
  mainImage          String?
  manualUrl          String?
  rentalPrice        Decimal?
  commercialMode     String           @default("venta")
  location           String?
  tags               String[]
  isFeatured         Boolean          @default(false)
  isVisible          Boolean          @default(true)
  technicalSheet     String?
  specifications     Json
  isActive           Boolean          @default(true)
  categoryId         String
  category           Category         @relation(fields: [categoryId], references: [id])
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  variants           ProductVariant[]
  orderItems         OrderItem[]
  quoteItems         QuoteItem[]
  favorites          Favorite[]
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt

  @@unique([terraqoWorkspaceId, slug])
  @@unique([terraqoWorkspaceId, sku])
  @@index([categoryId])
  @@index([brand])
  @@index([requiresQuote])
  @@index([stock])
  @@index([isActive])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model ProductVariant {
  id        String   @id @default(cuid())
  productId String
  name      String
  sku       String
  price     Decimal?
  stock     Int      @default(0)
  options   Json
  product   Product  @relation(fields: [productId], references: [id], onDelete: Cascade)

  @@unique([productId, sku])
  @@index([productId])
  @@schema("icc")
}

model Order {
  id                 String           @id @default(cuid())
  userId             String?
  user               User?            @relation(fields: [userId], references: [id])
  customerName       String
  customerEmail      String
  customerPhone      String?
  status             OrderStatus      @default(PENDING)
  total              Decimal
  currency           String           @default("USD")
  notes              String?
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  items              OrderItem[]
  addressId          String?
  address            Address?         @relation(fields: [addressId], references: [id])
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt

  @@index([status, createdAt])
  @@index([customerEmail])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model OrderItem {
  id        String  @id @default(cuid())
  orderId   String
  productId String
  quantity  Int
  unitPrice Decimal
  subtotal  Decimal
  order     Order   @relation(fields: [orderId], references: [id], onDelete: Cascade)
  product   Product @relation(fields: [productId], references: [id])

  @@index([orderId])
  @@index([productId])
  @@schema("icc")
}

model Address {
  id         String  @id @default(cuid())
  userId     String?
  user       User?   @relation(fields: [userId], references: [id])
  label      String?
  line1      String
  line2      String?
  city       String
  region     String?
  subdivisionCode String?
  country    String  @default("PE")
  postalCode String?
  orders     Order[]

  @@schema("icc")
}

model Favorite {
  id        String  @id @default(cuid())
  userId    String
  productId String
  user      User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  @@unique([userId, productId])
  @@schema("icc")
}

model Service {
  id                 String           @id @default(cuid())
  title              String
  slug               String
  category           String?
  categoryId         String?
  subcategoryId      String?
  categoryRef        ServiceCategory? @relation("ServiceCategoryPrimary", fields: [categoryId], references: [id], onDelete: SetNull)
  subcategoryRef     ServiceCategory? @relation("ServiceCategorySecondary", fields: [subcategoryId], references: [id], onDelete: SetNull)
  isFeatured         Boolean          @default(false)
  status             String           @default("ACTIVE")
  icon               String?
  cover              String?
  gallery            String[]         @default([])
  video              String?
  headline           String?
  summary            String
  benefits           String[]         @default([])
  applications       String[]         @default([])
  deliverables       String[]         @default([])
  technologies       String[]         @default([])
  precision          String?
  formats            String[]         @default([])
  compatibility      String[]         @default([])
  seoTitle           String?
  metaDescription    String?
  ogImage            String?
  relatedProjects    String[]         @default([])
  successCases       String[]         @default([])
  relatedServices    String[]         @default([])
  sectorSlugs        String[]         @default([])
  content            Json
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  isPublished        Boolean          @default(true)
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt

  @@unique([terraqoWorkspaceId, slug])
  @@index([isPublished, isFeatured])
  @@index([category])
  @@index([categoryId])
  @@index([subcategoryId])
  @@index([status])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model Sector {
  id                 String           @id @default(cuid())
  name               String
  slug               String
  description        String
  icon               String?
  image              String?
  position           Int              @default(0)
  active             Boolean          @default(true)
  seoTitle           String?
  metaDescription    String?
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt

  @@unique([terraqoWorkspaceId, slug])
  @@index([active])
  @@index([position])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model ServiceCategory {
  id                  String            @id @default(cuid())
  name                String
  slug                String
  description         String?
  icon                String?
  seoTitle            String?
  metaDescription     String?
  position            Int               @default(0)
  active              Boolean           @default(true)
  parentId            String?
  parent              ServiceCategory?  @relation("ServiceCategoryTree", fields: [parentId], references: [id], onDelete: Cascade)
  children            ServiceCategory[] @relation("ServiceCategoryTree")
  services            Service[]         @relation("ServiceCategoryPrimary")
  subcategoryServices Service[]         @relation("ServiceCategorySecondary")
  terraqoWorkspaceId  String
  terraqoWorkspace    TerraqoWorkspace  @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt           DateTime          @default(now())
  updatedAt           DateTime          @updatedAt

  @@unique([terraqoWorkspaceId, slug])
  @@index([parentId])
  @@index([active])
  @@index([position])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model CmsPage {
  id                 String           @id @default(cuid())
  title              String
  slug               String
  metaTitle          String?
  metaDesc           String?
  content            Json
  isPublished        Boolean          @default(true)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt

  @@unique([terraqoWorkspaceId, slug])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model Banner {
  id                 String           @id @default(cuid())
  title              String
  subtitle           String?
  ctaLabel           String?
  ctaHref            String?
  image              String?
  placement          String
  active             Boolean          @default(true)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt          DateTime         @default(now())

  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model Testimonial {
  id                 String           @id @default(cuid())
  quote              String
  author             String
  company            String?
  role               String?
  active             Boolean          @default(true)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt          DateTime         @default(now())

  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model ClientLogo {
  id                 String           @id @default(cuid())
  name               String
  logoUrl            String
  website            String?
  sector             String?
  position           Int              @default(0)
  active             Boolean          @default(true)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt

  @@index([active, position])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model Faq {
  id                 String           @id @default(cuid())
  question           String
  answer             String
  category           String?
  origin             String           @default("manual")
  approved           Boolean          @default(true)
  usageCount         Int              @default(0)
  position           Int              @default(0)
  active             Boolean          @default(true)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt          DateTime         @default(now())

  @@index([active, approved, position])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model BlogPost {
  id                 String           @id @default(cuid())
  title              String
  slug               String
  excerpt            String
  content            Json
  author             String?
  category           String?
  metaTitle          String?
  metaDesc           String?
  publishedAt        DateTime?
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt

  @@unique([terraqoWorkspaceId, slug])
  @@index([publishedAt])
  @@index([category])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model Lead {
  id                 String           @id @default(cuid())
  clientId           String?
  client             Client?          @relation(fields: [clientId], references: [id], onDelete: SetNull)
  companyId          String?
  companyRef         Company?         @relation(fields: [companyId], references: [id], onDelete: SetNull)
  contactId          String?
  contact            Contact?         @relation(fields: [contactId], references: [id], onDelete: SetNull)
  assignedProfileId  String?
  assignedProfile    StaffProfile?    @relation("LeadAssignedProfile", fields: [assignedProfileId], references: [id], onDelete: SetNull)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  name               String
  email              String
  phone              String?
  company            String?
  message            String
  source             String?
  interest           String?
  priority           LeadPriority     @default(MEDIUM)
  estimatedValue     Decimal?
  nextFollowUpAt     DateTime?
  status             LeadStatus       @default(NEW)
  notes              LeadNote[]
  quotes             Quote[]
  opportunity        Opportunity?
  activities         ActivityLog[]
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt
  deletedAt          DateTime?

  @@index([status, createdAt])
  @@index([email])
  @@index([assignedProfileId])
  @@index([clientId])
  @@index([companyId])
  @@index([contactId])
  @@index([terraqoWorkspaceId])
  @@index([deletedAt])
  @@schema("icc")
}

model LeadNote {
  id        String   @id @default(cuid())
  leadId    String
  lead      Lead     @relation(fields: [leadId], references: [id], onDelete: Cascade)
  authorId  String?
  body      String
  createdAt DateTime @default(now())

  @@index([leadId, createdAt])
  @@schema("icc")
}

model Quote {
  id                 String           @id @default(cuid())
  number             String           @unique
  clientId           String?
  client             Client?          @relation(fields: [clientId], references: [id], onDelete: SetNull)
  companyId          String?
  companyRef         Company?         @relation(fields: [companyId], references: [id], onDelete: SetNull)
  contactId          String?
  contact            Contact?         @relation(fields: [contactId], references: [id], onDelete: SetNull)
  opportunityId      String?
  opportunity        Opportunity?     @relation(fields: [opportunityId], references: [id], onDelete: SetNull)
  leadId             String?
  lead               Lead?            @relation(fields: [leadId], references: [id], onDelete: SetNull)
  sellerProfileId    String?
  sellerProfile      StaffProfile?    @relation("QuoteSellerProfile", fields: [sellerProfileId], references: [id], onDelete: SetNull)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  publicToken        String?          @unique @default(cuid())
  customerName       String
  customerEmail      String?
  company            String?
  status             QuoteStatus      @default(DRAFT)
  currency           String           @default("USD")
  subtotal           Decimal          @default(0)
  discount           Decimal          @default(0)
  tax                Decimal          @default(0)
  total              Decimal          @default(0)
  validUntil         DateTime?
  terms              String?
  deliveryTime       String?
  observations       String?
  viewedAt           DateTime?
  acceptedAt         DateTime?
  rejectedAt         DateTime?
  items              QuoteItem[]
  commissions        Commission[]
  sale               Sale?
  activities         ActivityLog[]
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt
  deletedAt          DateTime?

  @@index([status, createdAt])
  @@index([sellerProfileId])
  @@index([clientId])
  @@index([companyId])
  @@index([contactId])
  @@index([opportunityId])
  @@index([terraqoWorkspaceId])
  @@index([deletedAt])
  @@schema("icc")
}

model QuoteItem {
  id          String   @id @default(cuid())
  quoteId     String
  quote       Quote    @relation(fields: [quoteId], references: [id], onDelete: Cascade)
  productId   String?
  product     Product? @relation(fields: [productId], references: [id], onDelete: SetNull)
  type        String   @default("product")
  description String
  quantity    Int      @default(1)
  unitPrice   Decimal  @default(0)
  discount    Decimal  @default(0)
  subtotal    Decimal  @default(0)

  @@index([quoteId])
  @@index([productId])
  @@schema("icc")
}

model Commission {
  id                 String           @id @default(cuid())
  quoteId            String?
  quote              Quote?           @relation(fields: [quoteId], references: [id], onDelete: SetNull)
  sellerProfileId    String
  sellerProfile      StaffProfile     @relation("CommissionSellerProfile", fields: [sellerProfileId], references: [id], onDelete: Cascade)
  type               CommissionType   @default(SALE_PERCENTAGE)
  baseAmount         Decimal          @default(0)
  rate               Decimal          @default(0)
  amount             Decimal          @default(0)
  status             CommissionStatus @default(PENDING)
  paidAt             DateTime?
  notes              String?
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt

  @@index([sellerProfileId, status])
  @@index([quoteId])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model Opportunity {
  id                 String            @id @default(cuid())
  code               String            @unique
  title              String
  companyId          String
  company            Company           @relation(fields: [companyId], references: [id], onDelete: Cascade)
  contactId          String?
  contact            Contact?          @relation(fields: [contactId], references: [id], onDelete: SetNull)
  leadId             String?           @unique
  lead               Lead?             @relation(fields: [leadId], references: [id], onDelete: SetNull)
  sellerProfileId    String?
  sellerProfile      StaffProfile?     @relation("OpportunitySellerProfile", fields: [sellerProfileId], references: [id], onDelete: SetNull)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace  @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  status             OpportunityStatus @default(OPEN)
  source             String?
  interest           String?
  estimatedValue     Decimal?
  probability        Int               @default(25)
  nextStep           String?
  nextFollowUpAt     DateTime?
  notes              String?
  quotes             Quote[]
  sales              Sale[]
  activities         ActivityLog[]
  createdAt          DateTime          @default(now())
  updatedAt          DateTime          @updatedAt
  deletedAt          DateTime?
  projects           Project[]

  @@index([companyId])
  @@index([contactId])
  @@index([sellerProfileId])
  @@index([status, createdAt])
  @@index([terraqoWorkspaceId])
  @@index([deletedAt])
  @@schema("icc")
}

model Sale {
  id                 String           @id @default(cuid())
  number             String           @unique
  quoteId            String?          @unique
  quote              Quote?           @relation(fields: [quoteId], references: [id], onDelete: SetNull)
  opportunityId      String?
  opportunity        Opportunity?     @relation(fields: [opportunityId], references: [id], onDelete: SetNull)
  clientId           String?
  client             Client?          @relation(fields: [clientId], references: [id], onDelete: SetNull)
  companyId          String?
  company            Company?         @relation(fields: [companyId], references: [id], onDelete: SetNull)
  contactId          String?
  contact            Contact?         @relation(fields: [contactId], references: [id], onDelete: SetNull)
  sellerProfileId    String?
  sellerProfile      StaffProfile?    @relation("SaleSellerProfile", fields: [sellerProfileId], references: [id], onDelete: SetNull)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  status             SaleStatus       @default(PENDING)
  currency           String           @default("USD")
  amount             Decimal          @default(0)
  commissionAmount   Decimal          @default(0)
  paidAt             DateTime?
  projects           Project[]
  activities         ActivityLog[]
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt
  deletedAt          DateTime?

  @@index([status, createdAt])
  @@index([companyId])
  @@index([clientId])
  @@index([sellerProfileId])
  @@index([terraqoWorkspaceId])
  @@index([deletedAt])
  @@schema("icc")
}

model ContactMessage {
  id                 String           @id @default(cuid())
  name               String
  email              String
  phone              String?
  company            String?
  message            String
  intent             String?
  context            String?
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt          DateTime         @default(now())

  @@index([createdAt])
  @@index([email])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model ChatConversation {
  id                 String           @id @default(cuid())
  customerName       String
  customerEmail      String?
  customerPhone      String?
  topic              String?
  status             ChatStatus       @default(WAITING)
  assignedToId       String?
  assignedTo         User?            @relation(fields: [assignedToId], references: [id])
  assignedProfileId  String?
  assignedProfile    StaffProfile?    @relation(fields: [assignedProfileId], references: [id], onDelete: SetNull)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  messages           ChatMessage[]
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt

  @@index([status, createdAt])
  @@index([assignedToId])
  @@index([assignedProfileId])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model ChatMessage {
  id             String           @id @default(cuid())
  conversationId String
  conversation   ChatConversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  sender         String
  body           String
  createdAt      DateTime         @default(now())

  @@index([conversationId, createdAt])
  @@schema("icc")
}

model InternalChatChannel {
  id                 String                @id @default(cuid())
  name               String
  slug               String
  description        String?
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace      @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  messages           InternalChatMessage[]
  createdAt          DateTime              @default(now())
  updatedAt          DateTime              @updatedAt

  @@unique([terraqoWorkspaceId, slug])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model InternalChatMessage {
  id        String              @id @default(cuid())
  channelId String
  channel   InternalChatChannel @relation(fields: [channelId], references: [id], onDelete: Cascade)
  userId    String?
  user      User?               @relation(fields: [userId], references: [id], onDelete: SetNull)
  body      String
  files     String[]
  leadId    String?
  projectId String?
  ticketId  String?
  createdAt DateTime            @default(now())

  @@index([channelId, createdAt])
  @@index([userId])
  @@schema("icc")
}

model BotConversation {
  id                 String           @id @default(cuid())
  customerName       String?
  customerEmail      String?
  status             String           @default("bot")
  provider           BotProvider      @default(LOCAL)
  escalatedChatId    String?
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  messages           BotMessage[]
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt

  @@index([status, createdAt])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model BotMessage {
  id             String          @id @default(cuid())
  conversationId String
  conversation   BotConversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  sender         String
  body           String
  source         String?
  confidence     Decimal?
  createdAt      DateTime        @default(now())

  @@index([conversationId, createdAt])
  @@schema("icc")
}

model BotUnansweredQuestion {
  id                 String            @id @default(cuid())
  question           String
  answer             String?
  category           String?
  source             String?
  frequency          Int               @default(1)
  status             BotQuestionStatus @default(PENDING)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace  @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt          DateTime          @default(now())
  updatedAt          DateTime          @updatedAt

  @@unique([terraqoWorkspaceId, question])
  @@index([status, frequency])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model Notification {
  id                 String           @id @default(cuid())
  userId             String?
  user               User?            @relation(fields: [userId], references: [id], onDelete: Cascade)
  type               NotificationType @default(SYSTEM)
  title              String
  body               String?
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  href               String?
  readAt             DateTime?
  createdAt          DateTime         @default(now())

  @@index([userId, readAt, createdAt])
  @@index([type, createdAt])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model StaffProfile {
  id                 String                @id @default(cuid())
  userId             String?
  user               User?                 @relation(fields: [userId], references: [id], onDelete: SetNull)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace      @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  displayName        String
  email              String?
  phone              String?
  roleTitle          String
  department         StaffDepartment       @default(SALES)
  avatar             String?
  availability       TechnicalAvailability @default(AVAILABLE)
  workZone           String?
  experience         String?
  certifications     String[]
  documents          String[]
  commissionType     CommissionType        @default(SALE_PERCENTAGE)
  commissionRate     Decimal               @default(5)
  fixedCommission    Decimal               @default(0)
  monthlyGoal        Decimal               @default(0)
  territory          String?
  internalNotes      String?
  specialties        String[]
  tools              Json
  active             Boolean               @default(true)
  assignedChats      ChatConversation[]
  assignedLeads      Lead[]                @relation("LeadAssignedProfile")
  opportunities      Opportunity[]         @relation("OpportunitySellerProfile")
  quotes             Quote[]               @relation("QuoteSellerProfile")
  sales              Sale[]                @relation("SaleSellerProfile")
  commissions        Commission[]          @relation("CommissionSellerProfile")
  projectMembers     ProjectMember[]
  tasks              Task[]                @relation("TaskAssigneeProfile")
  ticketAssignee     Ticket[]              @relation("TicketAssigneeProfile")
  progressEntries    ProjectProgress[]
  createdAt          DateTime              @default(now())
  updatedAt          DateTime              @updatedAt

  @@unique([terraqoWorkspaceId, userId])
  @@unique([terraqoWorkspaceId, email])
  @@index([department, active])
  @@index([terraqoWorkspaceId])
  @@schema("icc")
}

model Project {
  id                   String                          @id @default(cuid())
  title                String
  slug                 String
  clientId             String?
  client               Client?                         @relation(fields: [clientId], references: [id], onDelete: SetNull)
  companyId            String?
  company              Company?                        @relation(fields: [companyId], references: [id], onDelete: SetNull)
  opportunityId        String?
  opportunity          Opportunity?                    @relation(fields: [opportunityId], references: [id], onDelete: SetNull)
  saleId               String?
  sale                 Sale?                           @relation(fields: [saleId], references: [id], onDelete: SetNull)
  terraqoWorkspaceId   String
  terraqoWorkspace     TerraqoWorkspace                @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  clientName           String?
  location             String?
  category             String?
  servicesApplied      String[]
  summary              String
  description          String
  challenge            String?
  solution             String?
  results              String?
  status               ProjectStatus                   @default(PLANNING)
  isPublic             Boolean                         @default(false)
  isFeatured           Boolean                         @default(false)
  images               ProjectImage[]
  members              ProjectMember[]
  progress             ProjectProgress[]
  milestones           Milestone[]
  tasks                Task[]
  documents            Document[]
  activities           ActivityLog[]
  terraqoExperiences   TerraqoProfessionalExperience[]
  terraqoJobPosts      TerraqoJobPost[]
  terraqoWorklogs      TerraqoWorklogEntry[]
  terraqoConversations TerraqoConversation[]
  terraqoMeetings      TerraqoMeeting[]
  terraqoTeams         TerraqoTeam[]
  worklogValidations   TerraqoWorklogValidation[]
  attendanceEvents     TerraqoAttendanceEvent[]
  latitude             Float?
  longitude            Float?
  geofenceRadiusMeters Int                             @default(250)
  createdAt            DateTime                        @default(now())
  updatedAt            DateTime                        @updatedAt
  deletedAt            DateTime?

  @@unique([terraqoWorkspaceId, slug])
  @@index([status, isPublic])
  @@index([isFeatured])
  @@index([clientId])
  @@index([companyId])
  @@index([opportunityId])
  @@index([terraqoWorkspaceId])
  @@index([deletedAt])
  @@schema("icc")
}

model ProjectImage {
  id        String   @id @default(cuid())
  projectId String
  project   Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  url       String
  alt       String?
  position  Int      @default(0)
  createdAt DateTime @default(now())

  @@index([projectId, position])
  @@schema("icc")
}

model ProjectMember {
  id             String       @id @default(cuid())
  projectId      String
  project        Project      @relation(fields: [projectId], references: [id], onDelete: Cascade)
  staffProfileId String
  staffProfile   StaffProfile @relation(fields: [staffProfileId], references: [id], onDelete: Cascade)
  role           ProjectRole  @default(SURVEYOR)
  createdAt      DateTime     @default(now())

  @@unique([projectId, staffProfileId, role])
  @@index([staffProfileId])
  @@schema("icc")
}

model ProjectProgress {
  id             String        @id @default(cuid())
  projectId      String
  project        Project       @relation(fields: [projectId], references: [id], onDelete: Cascade)
  staffProfileId String?
  staffProfile   StaffProfile? @relation(fields: [staffProfileId], references: [id], onDelete: SetNull)
  title          String
  body           String
  milestone      String?
  files          String[]
  createdAt      DateTime      @default(now())

  @@index([projectId, createdAt])
  @@index([staffProfileId])
  @@schema("icc")
}

model Milestone {
  id          String          @id @default(cuid())
  projectId   String
  project     Project         @relation(fields: [projectId], references: [id], onDelete: Cascade)
  title       String
  description String?
  status      MilestoneStatus @default(PENDING)
  dueDate     DateTime?
  completedAt DateTime?
  tasks       Task[]
  documents   Document[]
  createdAt   DateTime        @default(now())
  updatedAt   DateTime        @updatedAt
  deletedAt   DateTime?

  @@index([projectId, status])
  @@index([dueDate])
  @@index([deletedAt])
  @@schema("icc")
}

model Task {
  id                String        @id @default(cuid())
  projectId         String
  project           Project       @relation(fields: [projectId], references: [id], onDelete: Cascade)
  milestoneId       String?
  milestone         Milestone?    @relation(fields: [milestoneId], references: [id], onDelete: SetNull)
  assignedProfileId String?
  assignedProfile   StaffProfile? @relation("TaskAssigneeProfile", fields: [assignedProfileId], references: [id], onDelete: SetNull)
  title             String
  description       String?
  status            TaskStatus    @default(TODO)
  dueDate           DateTime?
  completedAt       DateTime?
  documents         Document[]
  activities        ActivityLog[]
  createdAt         DateTime      @default(now())
  updatedAt         DateTime      @updatedAt
  deletedAt         DateTime?

  @@index([projectId, status])
  @@index([milestoneId])
  @@index([assignedProfileId])
  @@index([dueDate])
  @@index([deletedAt])
  @@schema("icc")
}

model Ticket {
  id                 String           @id @default(cuid())
  code               String           @unique
  clientId           String?
  client             Client?          @relation(fields: [clientId], references: [id], onDelete: SetNull)
  companyId          String?
  companyRef         Company?         @relation(fields: [companyId], references: [id], onDelete: SetNull)
  contactId          String?
  contact            Contact?         @relation(fields: [contactId], references: [id], onDelete: SetNull)
  assignedProfileId  String?
  assignedProfile    StaffProfile?    @relation("TicketAssigneeProfile", fields: [assignedProfileId], references: [id], onDelete: SetNull)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  customerName       String
  customerEmail      String
  company            String?
  subject            String
  category           TicketCategory   @default(TECHNICAL_QUERY)
  priority           TicketPriority   @default(MEDIUM)
  status             TicketStatus     @default(OPEN)
  description        String
  attachments        String[]
  messages           TicketMessage[]
  documents          Document[]
  activities         ActivityLog[]
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt
  closedAt           DateTime?
  deletedAt          DateTime?

  @@index([status, createdAt])
  @@index([clientId])
  @@index([companyId])
  @@index([contactId])
  @@index([assignedProfileId])
  @@index([customerEmail])
  @@index([terraqoWorkspaceId])
  @@index([deletedAt])
  @@schema("icc")
}

model TicketMessage {
  id        String   @id @default(cuid())
  ticketId  String
  ticket    Ticket   @relation(fields: [ticketId], references: [id], onDelete: Cascade)
  authorId  String?
  author    User?    @relation(fields: [authorId], references: [id], onDelete: SetNull)
  sender    String
  body      String
  files     String[]
  createdAt DateTime @default(now())

  @@index([ticketId, createdAt])
  @@index([authorId])
  @@schema("icc")
}

model ClientDocument {
  id        String   @id @default(cuid())
  clientId  String
  client    Client   @relation(fields: [clientId], references: [id], onDelete: Cascade)
  title     String
  url       String
  type      String?
  createdAt DateTime @default(now())

  @@index([clientId, createdAt])
  @@schema("icc")
}

model Document {
  id                 String           @id @default(cuid())
  title              String
  url                String
  type               String?
  visibility         String           @default("internal")
  companyId          String?
  company            Company?         @relation(fields: [companyId], references: [id], onDelete: SetNull)
  contactId          String?
  contact            Contact?         @relation(fields: [contactId], references: [id], onDelete: SetNull)
  projectId          String?
  project            Project?         @relation(fields: [projectId], references: [id], onDelete: Cascade)
  milestoneId        String?
  milestone          Milestone?       @relation(fields: [milestoneId], references: [id], onDelete: SetNull)
  taskId             String?
  task               Task?            @relation(fields: [taskId], references: [id], onDelete: SetNull)
  ticketId           String?
  ticket             Ticket?          @relation(fields: [ticketId], references: [id], onDelete: SetNull)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt
  deletedAt          DateTime?

  @@index([companyId])
  @@index([projectId])
  @@index([ticketId])
  @@index([visibility])
  @@index([terraqoWorkspaceId])
  @@index([deletedAt])
  @@schema("icc")
}

model ActivityLog {
  id                 String           @id @default(cuid())
  actorId            String?
  actor              User?            @relation(fields: [actorId], references: [id], onDelete: SetNull)
  action             ActivityAction
  entityType         String
  entityId           String
  title              String
  body               String?
  metadata           Json?
  companyId          String?
  company            Company?         @relation(fields: [companyId], references: [id], onDelete: SetNull)
  contactId          String?
  contact            Contact?         @relation(fields: [contactId], references: [id], onDelete: SetNull)
  leadId             String?
  lead               Lead?            @relation(fields: [leadId], references: [id], onDelete: SetNull)
  opportunityId      String?
  opportunity        Opportunity?     @relation(fields: [opportunityId], references: [id], onDelete: SetNull)
  quoteId            String?
  quote              Quote?           @relation(fields: [quoteId], references: [id], onDelete: SetNull)
  saleId             String?
  sale               Sale?            @relation(fields: [saleId], references: [id], onDelete: SetNull)
  projectId          String?
  project            Project?         @relation(fields: [projectId], references: [id], onDelete: SetNull)
  taskId             String?
  task               Task?            @relation(fields: [taskId], references: [id], onDelete: SetNull)
  ticketId           String?
  ticket             Ticket?          @relation(fields: [ticketId], references: [id], onDelete: SetNull)
  terraqoWorkspaceId String
  terraqoWorkspace   TerraqoWorkspace @relation(fields: [terraqoWorkspaceId], references: [id], onDelete: Restrict)
  createdAt          DateTime         @default(now())

  @@index([entityType, entityId])
  @@index([companyId, createdAt])
  @@index([leadId, createdAt])
  @@index([opportunityId, createdAt])
  @@index([quoteId, createdAt])
  @@index([saleId, createdAt])
  @@index([projectId, createdAt])
  @@index([ticketId, createdAt])
  @@index([terraqoWorkspaceId, createdAt])
  @@schema("icc")
}

model TerraqoWorkspace {
  id                       String                           @id @default(cuid())
  name                     String
  slug                     String                           @unique
  type                     TerraqoWorkspaceType             @default(CLIENT_COMPANY)
  industry                 String?
  description              String?
  companyId                String?
  company                  Company?                         @relation(fields: [companyId], references: [id], onDelete: SetNull)
  ownerUserId              String?
  owner                    User?                            @relation("TerraqoWorkspaceOwner", fields: [ownerUserId], references: [id], onDelete: SetNull)
  brandName                String?
  publicSlug               String?
  domain                   String?
  logoUrl                  String?
  country                  String                           @default("PE")
  region                   String?
  locationSubdivisionCode  String?
  locationCity             String?
  settings                 Json?
  active                   Boolean                          @default(true)
  modules                  TerraqoWorkspaceModule[]
  subscriptions            TerraqoSubscription[]
  members                  TerraqoWorkspaceMember[]
  companies                Company[]                        @relation("TerraqoWorkspaceCompanies")
  contacts                 Contact[]
  clientAccounts           ClientAccount[]
  clients                  Client[]
  products                 Product[]
  orders                   Order[]
  services                 Service[]
  serviceCategories        ServiceCategory[]
  categories               Category[]
  sectors                  Sector[]
  cmsPages                 CmsPage[]
  banners                  Banner[]
  testimonials             Testimonial[]
  faqs                     Faq[]
  blogPosts                BlogPost[]
  clientLogos              ClientLogo[]
  leads                    Lead[]
  quotes                   Quote[]
  opportunities            Opportunity[]
  sales                    Sale[]
  commissions              Commission[]
  contactMessages          ContactMessage[]
  chatConversations        ChatConversation[]
  internalChannels         InternalChatChannel[]
  botConversations         BotConversation[]
  botQuestions             BotUnansweredQuestion[]
  notifications            Notification[]
  staffProfiles            StaffProfile[]
  projects                 Project[]
  tickets                  Ticket[]
  documents                Document[]
  activities               ActivityLog[]
  jobPosts                 TerraqoJobPost[]
  jobApplications          TerraqoProjectApplication[]
  professionalAffiliations TerraqoProfessionalAffiliation[]
  professionalDocuments    TerraqoProfessionalDocument[]
  forumChannels            TerraqoForumChannel[]
  worklogs                 TerraqoWorklogEntry[]
  directConversations      TerraqoConversation[]
  meetings                 TerraqoMeeting[]
  teams                    TerraqoTeam[]
  privateNotes             TerraqoPrivateNote[]
  workspaceFiles           TerraqoWorkspaceFile[]
  webAuthnChallenges       TerraqoWebAuthnChallenge[]
  worklogValidations       TerraqoWorklogValidation[]
  attendanceEvents         TerraqoAttendanceEvent[]
  createdAt                DateTime                         @default(now())
  updatedAt                DateTime                         @updatedAt
  deletedAt                DateTime?

  @@index([publicSlug])
  @@index([type, active])
  @@index([country, locationSubdivisionCode, locationCity])
  @@index([companyId])
  @@index([ownerUserId])
  @@index([deletedAt])
  @@schema("icc")
}

model TerraqoConversation {
  id            String                           @id @default(cuid())
  type          TerraqoConversationType          @default(DIRECT)
  title         String?
  directKey     String?                          @unique
  workspaceId   String?
  workspace     TerraqoWorkspace?                @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  projectId     String?
  project       Project?                         @relation(fields: [projectId], references: [id], onDelete: SetNull)
  createdById   String
  createdBy     User                             @relation("TerraqoConversationCreator", fields: [createdById], references: [id], onDelete: Cascade)
  participants  TerraqoConversationParticipant[]
  messages      TerraqoDirectMessage[]
  meetings      TerraqoMeeting[]
  team          TerraqoTeam?
  lastMessageAt DateTime?
  archivedAt    DateTime?
  createdAt     DateTime                         @default(now())
  updatedAt     DateTime                         @updatedAt

  @@index([workspaceId, updatedAt])
  @@index([projectId])
  @@index([type, updatedAt])
  @@schema("icc")
}

model TerraqoTeam {
  id             String               @id @default(cuid())
  workspaceId    String
  workspace      TerraqoWorkspace     @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  ownerUserId    String
  owner          User                 @relation("TerraqoTeamOwner", fields: [ownerUserId], references: [id], onDelete: Cascade)
  name           String
  purpose        String
  status         TerraqoTeamStatus    @default(ACTIVE)
  projectId      String?
  project        Project?             @relation(fields: [projectId], references: [id], onDelete: SetNull)
  conversationId String?              @unique
  conversation   TerraqoConversation? @relation(fields: [conversationId], references: [id], onDelete: SetNull)
  members        TerraqoTeamMember[]
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt

  @@index([workspaceId, status, updatedAt])
  @@index([ownerUserId, status])
  @@index([projectId])
  @@schema("icc")
}

model TerraqoTeamMember {
  id              String                  @id @default(cuid())
  teamId          String
  team            TerraqoTeam             @relation(fields: [teamId], references: [id], onDelete: Cascade)
  userId          String
  user            User                    @relation("TerraqoTeamMemberUser", fields: [userId], references: [id], onDelete: Cascade)
  role            TerraqoTeamRole         @default(MEMBER)
  status          TerraqoTeamMemberStatus @default(INVITED)
  invitedByUserId String
  invitedBy       User                    @relation("TerraqoTeamMemberInviter", fields: [invitedByUserId], references: [id], onDelete: Cascade)
  invitedAt       DateTime                @default(now())
  joinedAt        DateTime?
  createdAt       DateTime                @default(now())
  updatedAt       DateTime                @updatedAt

  @@unique([teamId, userId])
  @@index([userId, status, updatedAt])
  @@index([teamId, status])
  @@schema("icc")
}

model TerraqoConversationParticipant {
  id             String                             @id @default(cuid())
  conversationId String
  conversation   TerraqoConversation                @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  userId         String
  user           User                               @relation(fields: [userId], references: [id], onDelete: Cascade)
  role           TerraqoConversationParticipantRole @default(MEMBER)
  lastReadAt     DateTime?
  mutedAt        DateTime?
  leftAt         DateTime?
  joinedAt       DateTime                           @default(now())

  @@unique([conversationId, userId])
  @@index([userId, leftAt])
  @@schema("icc")
}

model TerraqoDirectMessage {
  id             String              @id @default(cuid())
  conversationId String
  conversation   TerraqoConversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  senderId       String
  sender         User                @relation("TerraqoDirectMessageSender", fields: [senderId], references: [id], onDelete: Cascade)
  body           String
  attachments    String[]            @default([])
  editedAt       DateTime?
  deletedAt      DateTime?
  createdAt      DateTime            @default(now())

  @@index([conversationId, createdAt])
  @@index([senderId, createdAt])
  @@schema("icc")
}

model TerraqoMeeting {
  id             String                      @id @default(cuid())
  roomKey        String                      @unique
  title          String?
  status         TerraqoMeetingStatus        @default(LIVE)
  provider       TerraqoMeetingProvider      @default(JITSI)
  workspaceId    String
  workspace      TerraqoWorkspace            @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  conversationId String
  conversation   TerraqoConversation         @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  projectId      String?
  project        Project?                    @relation(fields: [projectId], references: [id], onDelete: SetNull)
  createdById    String
  createdBy      User                        @relation("TerraqoMeetingCreator", fields: [createdById], references: [id], onDelete: Cascade)
  participants   TerraqoMeetingParticipant[]
  startedAt      DateTime                    @default(now())
  endedAt        DateTime?
  createdAt      DateTime                    @default(now())
  updatedAt      DateTime                    @updatedAt

  @@index([workspaceId, status, createdAt])
  @@index([conversationId, status])
  @@index([projectId])
  @@schema("icc")
}

model TerraqoMeetingParticipant {
  id        String         @id @default(cuid())
  meetingId String
  meeting   TerraqoMeeting @relation(fields: [meetingId], references: [id], onDelete: Cascade)
  userId    String
  user      User           @relation(fields: [userId], references: [id], onDelete: Cascade)
  joinedAt  DateTime?
  leftAt    DateTime?
  createdAt DateTime       @default(now())

  @@unique([meetingId, userId])
  @@index([userId, createdAt])
  @@schema("icc")
}

model TerraqoWorkspaceModule {
  id          String            @id @default(cuid())
  workspaceId String
  workspace   TerraqoWorkspace  @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  code        TerraqoModuleCode
  active      Boolean           @default(false)
  config      Json?
  enabledAt   DateTime?
  disabledAt  DateTime?
  createdAt   DateTime          @default(now())
  updatedAt   DateTime          @updatedAt

  @@unique([workspaceId, code])
  @@index([code, active])
  @@schema("icc")
}

model TerraqoSubscription {
  id          String                    @id @default(cuid())
  workspaceId String
  workspace   TerraqoWorkspace          @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  tier        TerraqoPlanTier           @default(BASIC)
  status      TerraqoSubscriptionStatus @default(TRIALING)
  seats       Int                       @default(3)
  startsAt    DateTime                  @default(now())
  renewsAt    DateTime?
  cancelledAt DateTime?
  notes       String?
  createdAt   DateTime                  @default(now())
  updatedAt   DateTime                  @updatedAt

  @@index([workspaceId, status])
  @@index([tier, status])
  @@schema("icc")
}

model TerraqoWorkspaceMember {
  id          String            @id @default(cuid())
  workspaceId String
  workspace   TerraqoWorkspace  @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  userId      String
  user        User              @relation(fields: [userId], references: [id], onDelete: Cascade)
  role        TerraqoMemberRole @default(MEMBER)
  title       String?
  active      Boolean           @default(true)
  invitedAt   DateTime?
  joinedAt    DateTime?
  createdAt   DateTime          @default(now())
  updatedAt   DateTime          @updatedAt

  @@unique([workspaceId, userId])
  @@index([userId, active])
  @@index([workspaceId, role])
  @@schema("icc")
}

model TerraqoProfessionalProfile {
  id                         String                            @id @default(cuid())
  userId                     String                            @unique
  user                       User                              @relation(fields: [userId], references: [id], onDelete: Cascade)
  username                   String?                           @unique
  headline                   String?
  bio                        String?
  city                       String?
  country                    String                            @default("PE")
  region                     String?
  locationSubdivisionCode    String?
  locationCity               String?
  status                     TerraqoProfessionalStatus         @default(OPEN_TO_PROJECTS)
  visibility                 TerraqoVisibility                 @default(PRIVATE)
  yearsExperience            Int?
  professionalCategories     String[]                          @default([])
  specialties                String[]                          @default([])
  equipment                  String[]                          @default([])
  software                   String[]                          @default([])
  certifications             String[]                          @default([])
  portfolioUrl               String?
  cvUrl                      String?
  liveCvEnabled              Boolean                           @default(false)
  liveCvVisibility           TerraqoVisibility                 @default(PRIVATE)
  messagePrivacy             TerraqoMessagePrivacy             @default(WORKSPACE)
  friendDiscoveryEnabled     Boolean                           @default(true)
  bankAccountHolder          String?
  bankName                   String?
  bankAccountNumber          String?
  bankCci                    String?
  yapePhone                  String?
  plinPhone                  String?
  paymentNotes               String?
  generatedSummary           String?
  generatedSummaryUpdatedAt  DateTime?
  termsAcceptedAt            DateTime?
  termsVersion               String?
  privacyAcceptedAt          DateTime?
  onboardingSource           String?
  identityVerificationStatus TerraqoIdentityVerificationStatus @default(PENDING_DOCUMENTS)
  identitySubmittedAt        DateTime?
  identityVerifiedAt         DateTime?
  identityVerificationNote   String?
  experiences                TerraqoProfessionalExperience[]
  education                  TerraqoProfessionalEducation[]
  affiliations               TerraqoProfessionalAffiliation[]
  applications               TerraqoProjectApplication[]
  documents                  TerraqoProfessionalDocument[]
  worklogs                   TerraqoWorklogEntry[]
  attendanceEvents           TerraqoAttendanceEvent[]
  createdAt                  DateTime                          @default(now())
  updatedAt                  DateTime                          @updatedAt

  @@index([status, visibility])
  @@index([city])
  @@index([country, locationSubdivisionCode, locationCity])
  @@index([identityVerificationStatus])
  @@schema("icc")
}

model TerraqoProfessionalDocument {
  id                    String                          @id @default(cuid())
  professionalProfileId String
  professionalProfile   TerraqoProfessionalProfile      @relation(fields: [professionalProfileId], references: [id], onDelete: Cascade)
  workspaceId           String
  workspace             TerraqoWorkspace                @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  type                  TerraqoProfessionalDocumentType
  storageKey            String                          @unique
  fileName              String
  contentType           String
  size                  Int
  reviewStatus          TerraqoDocumentReviewStatus     @default(SUBMITTED)
  uploadedAt            DateTime                        @default(now())
  reviewedAt            DateTime?
  reviewedByUserId      String?
  reviewNote            String?

  @@index([professionalProfileId, type, uploadedAt])
  @@index([workspaceId, reviewStatus])
  @@schema("icc")
}

model TerraqoPrivateNote {
  id            String           @id @default(cuid())
  userId        String
  user          User             @relation(fields: [userId], references: [id], onDelete: Cascade)
  workspaceId   String
  workspace     TerraqoWorkspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  kind          TerraqoNoteKind  @default(SIMPLE)
  title         String?
  body          String?
  securePayload String?
  createdAt     DateTime         @default(now())
  updatedAt     DateTime         @updatedAt

  @@index([userId, workspaceId, updatedAt])
  @@schema("icc")
}

model TerraqoWorkspaceFile {
  id          String                @id @default(cuid())
  userId      String
  user        User                  @relation(fields: [userId], references: [id], onDelete: Cascade)
  workspaceId String
  workspace   TerraqoWorkspace      @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  category    TerraqoFileCategory   @default(OTHER)
  visibility  TerraqoFileVisibility @default(PRIVATE)
  title       String
  description String?
  projectName String?
  storageKey  String                @unique
  fileName    String
  contentType String
  size        Int
  createdAt   DateTime              @default(now())
  updatedAt   DateTime              @updatedAt

  @@index([userId, workspaceId, createdAt])
  @@index([workspaceId, visibility, createdAt])
  @@schema("icc")
}

model TerraqoProfessionalAffiliation {
  id                    String                     @id @default(cuid())
  professionalProfileId String
  professionalProfile   TerraqoProfessionalProfile @relation(fields: [professionalProfileId], references: [id], onDelete: Cascade)
  workspaceId           String
  workspace             TerraqoWorkspace           @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  companyId             String?
  company               Company?                   @relation(fields: [companyId], references: [id], onDelete: SetNull)
  companyName           String
  roleTitle             String?
  current               Boolean                    @default(true)
  startedAt             DateTime?
  endedAt               DateTime?
  verificationStatus    String                     @default("DECLARED")
  visibility            TerraqoVisibility          @default(PRIVATE)
  createdAt             DateTime                   @default(now())
  updatedAt             DateTime                   @updatedAt

  @@index([professionalProfileId, current])
  @@index([workspaceId, verificationStatus])
  @@index([companyId])
  @@schema("icc")
}

model TerraqoProfessionalExperience {
  id                    String                     @id @default(cuid())
  professionalProfileId String
  professionalProfile   TerraqoProfessionalProfile @relation(fields: [professionalProfileId], references: [id], onDelete: Cascade)
  projectId             String?
  project               Project?                   @relation(fields: [projectId], references: [id], onDelete: SetNull)
  title                 String
  companyName           String?
  role                  String?
  location              String?
  country               String?                    @default("PE")
  locationSubdivisionCode String?
  locationCity          String?
  startedAt             DateTime?
  endedAt               DateTime?
  currentlyWorking      Boolean                    @default(false)
  verifiedByTerraqo     Boolean                    @default(false)
  verificationNote      String?
  verificationStatus    TerraqoProfileEntryVerificationStatus @default(NOT_REQUESTED)
  verificationRequestedAt DateTime?
  validatorUserId       String?
  validatorName         String?
  validatorEmail        String?
  visibility            TerraqoVisibility          @default(PRIVATE)
  evidence              String[]                   @default([])
  createdAt             DateTime                   @default(now())
  updatedAt             DateTime                   @updatedAt

  @@index([professionalProfileId, startedAt])
  @@index([professionalProfileId, verificationStatus])
  @@index([validatorUserId, verificationStatus])
  @@index([country, locationSubdivisionCode, locationCity])
  @@index([projectId])
  @@index([verifiedByTerraqo])
  @@schema("icc")
}

model TerraqoProfessionalEducation {
  id                    String                     @id @default(cuid())
  professionalProfileId String
  professionalProfile   TerraqoProfessionalProfile @relation(fields: [professionalProfileId], references: [id], onDelete: Cascade)
  institution           String
  degree                String
  field                 String?
  country               String?                    @default("PE")
  locationSubdivisionCode String?
  locationCity          String?
  startedAt             DateTime?
  endedAt               DateTime?
  currentlyStudying     Boolean                    @default(false)
  visibility            TerraqoVisibility          @default(PRIVATE)
  verificationStatus    TerraqoProfileEntryVerificationStatus @default(NOT_REQUESTED)
  verificationRequestedAt DateTime?
  validatorUserId       String?
  validatorName         String?
  validatorEmail        String?
  evidence              String[]                   @default([])
  createdAt             DateTime                   @default(now())
  updatedAt             DateTime                   @updatedAt

  @@index([professionalProfileId, startedAt])
  @@index([professionalProfileId, verificationStatus])
  @@index([validatorUserId, verificationStatus])
  @@index([country, locationSubdivisionCode, locationCity])
  @@schema("icc")
}

model TerraqoWorklogEntry {
  id                    String                     @id @default(cuid())
  professionalProfileId String
  professionalProfile   TerraqoProfessionalProfile @relation(fields: [professionalProfileId], references: [id], onDelete: Cascade)
  authorId              String
  author                User                       @relation("TerraqoWorklogAuthor", fields: [authorId], references: [id], onDelete: Cascade)
  workspaceId           String?
  workspace             TerraqoWorkspace?          @relation(fields: [workspaceId], references: [id], onDelete: SetNull)
  projectId             String?
  project               Project?                   @relation(fields: [projectId], references: [id], onDelete: SetNull)
  title                 String
  summary               String
  outcome               String?
  type                  TerraqoWorklogType         @default(FIELD_UPDATE)
  evidenceStatus        TerraqoEvidenceStatus      @default(DECLARED)
  visibility            TerraqoVisibility          @default(PRIVATE)
  skills                String[]                   @default([])
  evidenceUrls          String[]                   @default([])
  occurredAt            DateTime                   @default(now())
  comments              TerraqoWorklogComment[]
  reactions             TerraqoWorklogReaction[]
  media                 TerraqoWorklogMedia[]
  validations           TerraqoWorklogValidation[]
  createdAt             DateTime                   @default(now())
  updatedAt             DateTime                   @updatedAt
  deletedAt             DateTime?

  @@index([professionalProfileId, occurredAt])
  @@index([workspaceId, visibility, occurredAt])
  @@index([projectId])
  @@index([evidenceStatus])
  @@index([deletedAt])
  @@schema("icc")
}

model TerraqoWebAuthnCredential {
  id           String    @id @default(cuid())
  userId       String
  user         User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  credentialId String
  publicKey    Bytes
  counter      BigInt    @default(0)
  transports   String[]  @default([])
  rpId         String
  deviceName   String?
  backedUp     Boolean   @default(false)
  createdAt    DateTime  @default(now())
  lastUsedAt   DateTime?

  @@unique([credentialId, rpId])
  @@index([userId, rpId])
  @@schema("icc")
}

model TerraqoWebAuthnChallenge {
  id             String                 @id @default(cuid())
  userId         String
  user           User                   @relation(fields: [userId], references: [id], onDelete: Cascade)
  workspaceId    String?
  workspace      TerraqoWorkspace?      @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  purpose        TerraqoWebAuthnPurpose
  challenge      String                 @unique
  rpId           String
  expectedOrigin String
  payload        Json?
  expiresAt      DateTime
  consumedAt     DateTime?
  createdAt      DateTime               @default(now())

  @@index([userId, purpose, expiresAt])
  @@index([workspaceId, purpose, expiresAt])
  @@schema("icc")
}

model TerraqoWorklogValidation {
  id                String                         @id @default(cuid())
  worklogId         String
  worklog           TerraqoWorklogEntry            @relation(fields: [worklogId], references: [id], onDelete: Cascade)
  workspaceId       String
  workspace         TerraqoWorkspace               @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  projectId         String?
  project           Project?                       @relation(fields: [projectId], references: [id], onDelete: SetNull)
  requestedByUserId String
  requestedBy       User                           @relation("TerraqoValidationRequester", fields: [requestedByUserId], references: [id], onDelete: Cascade)
  validatorUserId   String
  validator         User                           @relation("TerraqoValidationSupervisor", fields: [validatorUserId], references: [id], onDelete: Restrict)
  status            TerraqoWorklogValidationStatus @default(REQUESTED)
  requestNote       String?
  responseNote      String?
  credentialId      String?
  requestedAt       DateTime                       @default(now())
  resolvedAt        DateTime?
  createdAt         DateTime                       @default(now())
  updatedAt         DateTime                       @updatedAt

  @@index([worklogId, status])
  @@index([workspaceId, status, requestedAt])
  @@index([validatorUserId, status, requestedAt])
  @@schema("icc")
}

model TerraqoAttendanceEvent {
  id                    String                     @id @default(cuid())
  userId                String
  user                  User                       @relation(fields: [userId], references: [id], onDelete: Cascade)
  professionalProfileId String
  professionalProfile   TerraqoProfessionalProfile @relation(fields: [professionalProfileId], references: [id], onDelete: Cascade)
  workspaceId           String
  workspace             TerraqoWorkspace           @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  projectId             String
  project               Project                    @relation(fields: [projectId], references: [id], onDelete: Cascade)
  type                  TerraqoAttendanceType
  status                TerraqoAttendanceStatus
  latitude              Float
  longitude             Float
  accuracyMeters        Float
  distanceMeters        Float
  geofenceRadiusMeters  Int
  credentialId          String?
  capturedAt            DateTime                   @default(now())
  createdAt             DateTime                   @default(now())

  @@index([userId, workspaceId, capturedAt])
  @@index([professionalProfileId, projectId, capturedAt])
  @@index([workspaceId, status, capturedAt])
  @@schema("icc")
}

model TerraqoWorklogMedia {
  id          String              @id @default(cuid())
  worklogId   String
  worklog     TerraqoWorklogEntry @relation(fields: [worklogId], references: [id], onDelete: Cascade)
  storageKey  String              @unique
  fileName    String
  contentType String
  size        Int
  sortOrder   Int                 @default(0)
  createdAt   DateTime            @default(now())

  @@index([worklogId, sortOrder])
  @@schema("icc")
}

model TerraqoWorklogComment {
  id        String              @id @default(cuid())
  worklogId String
  worklog   TerraqoWorklogEntry @relation(fields: [worklogId], references: [id], onDelete: Cascade)
  authorId  String
  author    User                @relation(fields: [authorId], references: [id], onDelete: Cascade)
  body      String
  createdAt DateTime            @default(now())
  updatedAt DateTime            @updatedAt
  deletedAt DateTime?

  @@index([worklogId, createdAt])
  @@index([authorId])
  @@schema("icc")
}

model TerraqoWorklogReaction {
  id        String              @id @default(cuid())
  worklogId String
  worklog   TerraqoWorklogEntry @relation(fields: [worklogId], references: [id], onDelete: Cascade)
  userId    String
  user      User                @relation(fields: [userId], references: [id], onDelete: Cascade)
  type      TerraqoReactionType @default(USEFUL)
  createdAt DateTime            @default(now())

  @@unique([worklogId, userId])
  @@index([userId])
  @@index([worklogId, type])
  @@schema("icc")
}

model TerraqoJobPost {
  id                     String                      @id @default(cuid())
  workspaceId            String
  workspace              TerraqoWorkspace            @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  projectId              String?
  project                Project?                    @relation(fields: [projectId], references: [id], onDelete: SetNull)
  title                  String
  slug                   String
  summary                String
  description            String
  location               String?
  modality               String?
  requiredSkills         String[]                    @default([])
  requiredTools          String[]                    @default([])
  professionalCategories String[]                    @default([])
  budgetRange            String?
  status                 String                      @default("OPEN")
  visibility             TerraqoVisibility           @default(COMMUNITY)
  applications           TerraqoProjectApplication[]
  createdAt              DateTime                    @default(now())
  updatedAt              DateTime                    @updatedAt
  deletedAt              DateTime?

  @@unique([workspaceId, slug])
  @@index([workspaceId, status])
  @@index([projectId])
  @@index([visibility])
  @@schema("icc")
}

model TerraqoProjectApplication {
  id                    String                      @id @default(cuid())
  workspaceId           String
  workspace             TerraqoWorkspace            @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  jobPostId             String?
  jobPost               TerraqoJobPost?             @relation(fields: [jobPostId], references: [id], onDelete: Cascade)
  professionalProfileId String?
  professionalProfile   TerraqoProfessionalProfile? @relation(fields: [professionalProfileId], references: [id], onDelete: SetNull)
  userId                String?
  user                  User?                       @relation(fields: [userId], references: [id], onDelete: SetNull)
  status                TerraqoApplicationStatus    @default(SUBMITTED)
  coverNote             String?
  proposedRate          String?
  availabilityNote      String?
  professionalCategory  String?
  formConfigVersion     String?
  customAnswers         Json?
  termsAcceptedAt       DateTime?
  termsVersion          String?
  privacyAcceptedAt     DateTime?
  source                String?
  createdAt             DateTime                    @default(now())
  updatedAt             DateTime                    @updatedAt

  @@unique([jobPostId, professionalProfileId])
  @@index([workspaceId, status, createdAt])
  @@index([status, createdAt])
  @@index([userId])
  @@schema("icc")
}

model TerraqoForumChannel {
  id          String             @id @default(cuid())
  workspaceId String?
  workspace   TerraqoWorkspace?  @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  name        String
  slug        String
  description String?
  visibility  TerraqoVisibility  @default(COMMUNITY)
  active      Boolean            @default(true)
  posts       TerraqoForumPost[]
  createdAt   DateTime           @default(now())
  updatedAt   DateTime           @updatedAt

  @@unique([workspaceId, slug])
  @@index([workspaceId, active])
  @@index([visibility])
  @@schema("icc")
}

model TerraqoForumPost {
  id         String              @id @default(cuid())
  channelId  String
  channel    TerraqoForumChannel @relation(fields: [channelId], references: [id], onDelete: Cascade)
  authorId   String?
  author     User?               @relation(fields: [authorId], references: [id], onDelete: SetNull)
  title      String
  body       String
  tags       String[]            @default([])
  visibility TerraqoVisibility   @default(COMMUNITY)
  pinned     Boolean             @default(false)
  replies    TerraqoForumReply[]
  createdAt  DateTime            @default(now())
  updatedAt  DateTime            @updatedAt
  deletedAt  DateTime?

  @@index([channelId, createdAt])
  @@index([authorId])
  @@index([visibility])
  @@schema("icc")
}

model TerraqoForumReply {
  id        String           @id @default(cuid())
  postId    String
  post      TerraqoForumPost @relation(fields: [postId], references: [id], onDelete: Cascade)
  authorId  String?
  author    User?            @relation(fields: [authorId], references: [id], onDelete: SetNull)
  body      String
  createdAt DateTime         @default(now())
  updatedAt DateTime         @updatedAt
  deletedAt DateTime?

  @@index([postId, createdAt])
  @@index([authorId])
  @@schema("icc")
}
